import { CleanupPossibleNumbers } from "./cleanup-possible-numbers";

describe(CleanupPossibleNumbers.name, () => {
  it("should create an instance", () => {
    expect(new CleanupPossibleNumbers()).toBeTruthy();
  });
});
