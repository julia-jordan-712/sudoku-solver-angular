import { OnlyPossibleNumber } from "./only-possible-number";

describe(OnlyPossibleNumber.name, () => {
  it("should create an instance", () => {
    expect(new OnlyPossibleNumber()).toBeTruthy();
  });
});
