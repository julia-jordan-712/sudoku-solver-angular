import { createActionGroup, emptyProps, props } from '@ngrx/store';

export const SudokuSolverActions = createActionGroup({
  source: 'SudokuSolver',
  events: {
    'Load SudokuSolvers': emptyProps(),
    
    
  }
});
